# ====================================================================
# CREDIT WALLET 2.0 - ENVIRONMENT VARIABLES TEMPLATE
# Copy this file to .env and adjust your secrets if needed:
# cp .env.example .env
# ====================================================================

# ====================================================================
# 1. DATABASE CONFIGURATION (CHỈ CẦN CHỈNH SỬA TẠI ĐÂY)
# DATABASE_URL sẽ tự động được tạo từ các thông số bên dưới.
# ====================================================================

# --- NGUỒN 1: DOCKER POSTGRES CỤC BỘ (Mặc định) ---
# POSTGRES_HOST=db
# POSTGRES_PORT=5432
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=[your_password_here]
# POSTGRES_DB=credit_wallet
# POSTGRES_SSLMODE=

# --- NGUỒN 2: SUPABASE CLOUD (Bỏ comment khi dùng Supabase) ---
POSTGRES_HOST=aws-0-[region].pooler.supabase.com
POSTGRES_PORT=6543
POSTGRES_USER=postgres.[project_ref]
POSTGRES_PASSWORD=[your_password_here]
POSTGRES_DB=postgres
POSTGRES_SSLMODE=require

# ====================================================================
# 2. DATABASE URL (Tùy chọn - chỉ dùng khi muốn ghi đè URL trực tiếp)
# Nếu để trống hoặc comment, hệ thống sẽ tự động ghép từ các mục ở phần 1.
# ====================================================================
# DATABASE_URL=

# ====================================================================
# 3. ETL & GOOGLE SHEETS DATA INGESTION
# ====================================================================
GOOGLE_SHEET_ID=16kks0eL-j7SNxBAR3NlU5n1viIEvTjg-fAu9yWC9mAk
